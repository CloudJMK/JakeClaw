# claw-code placeholder

Place the Claw-code checkout or submodule here during a real deployment.

This draft leaves the directory empty by default because the actual upstream URL,
branch, and authentication method depend on your environment.
